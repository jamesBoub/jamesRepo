draws a grid of points from file 'cereal.txt'. The grid can be manipulated, and the grid's state is saved to file as plaintext.

right click to delete grid square
e to re-make and reset the grid
space to completely delete the grid
escape to save the grid's state to file